build:
  dockerfile: ./Dockerfile
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({
      command: 'python',
      args: ['server.py'],
      env: { 
        "PYTHONUNBUFFERED": "1",
        "DEBUG": "1"
      }
    })
